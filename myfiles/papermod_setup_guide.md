# PaperMod 主题样式配置完整指南

## 📁 文件放置位置

PaperMod 提供了扩展 CSS 的机制，**无需修改主题源文件**。

### 目录结构

```
your-hugo-site/
├── assets/
│   └── css/
│       └── extended/
│           └── custom.css    ← 将自定义 CSS 放这里
├── static/
│   ├── logo.png
│   └── images/
├── content/
├── layouts/                   ← 可选：覆盖模板
├── themes/
│   └── PaperMod/
└── hugo.toml
```

### 创建文件

```bash
# 创建目录
mkdir -p assets/css/extended

# 创建自定义 CSS 文件
touch assets/css/extended/custom.css

# 将上面 Artifact 的内容复制进去
```

---

## 🎨 主要调整说明

### 1. 主题色更改

| 元素 | 原色 | 新色（品牌色）|
|------|------|--------------|
| 链接 | 蓝色系 | #009873 |
| 悬停 | 深蓝 | #006B52 |
| 标签背景 | 灰色 | #E6F7F3 |
| 选中文字 | 蓝色 | #009873 |
| 代码高亮 | 默认 | #009873 |

### 2. 字号调整

| 元素 | 原始大小 | 调整后 |
|------|---------|--------|
| 正文 | 16px | 15px |
| H1 标题 | 2rem | 1.8rem |
| H2 标题 | 1.6rem | 1.5rem |
| H3 标题 | 1.4rem | 1.25rem |
| 导航菜单 | 15px | 14px |
| 文章元信息 | 14px | 13px |
| 页脚 | 14px | 13px |
| 代码块 | 14px | 13px |

### 3. 字体设置

```css
/* 英文: Inter */
/* 中文: PingFang SC (Mac) / Microsoft YaHei (Win) */
/* 代码: JetBrains Mono / Fira Code */
```

---

## 📝 详细配置步骤

### 步骤 1: 创建自定义 CSS 文件

```bash
mkdir -p assets/css/extended
```

将 Artifact "PaperMod 自定义样式 - custom.css" 的内容保存到：
```
assets/css/extended/custom.css
```

### 步骤 2: 更新 hugo.toml

确保配置正确（无需额外配置，PaperMod 会自动加载 extended 目录下的 CSS）：

```toml
[params]
  # 其他配置...
  
  # Logo 配置
  [params.label]
    text = "AioGeoLab"
    icon = "/logo.png"
    iconHeight = 36
```

### 步骤 3: 重启服务器

```bash
# 清除缓存
rm -rf public/ resources/

# 重启
hugo server -D
```

---

## 🎯 自定义调整

### 如果想要更小/更大的字号

编辑 `custom.css` 中的字号部分：

```css
/* 更小一号 (整体 14px 基准) */
body {
  font-size: 14px;
}

/* 更大一号 (整体 16px 基准) */
body {
  font-size: 16px;
}
```

### 如果想更换主题色

修改 CSS 变量：

```css
:root {
  /* 更换为其他颜色 */
  --brand-primary: #你的颜色;
  --brand-secondary: #你的辅助色;
  --brand-dark: #深色版本;
  --brand-light: #浅色背景;
}
```

### 如果想更换字体

**方案 A: 使用系统字体（推荐，加载快）**

```css
body {
  font-family: 
    -apple-system,
    BlinkMacSystemFont,
    'Segoe UI',
    'PingFang SC',
    'Microsoft YaHei',
    sans-serif;
}
```

**方案 B: 使用 Google Fonts**

```css
/* 在 CSS 开头添加 */
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400;500;700&display=swap');

body {
  font-family: 'Noto Sans SC', sans-serif;
}
```

**方案 C: 使用本地字体**

```css
/* 在 CSS 开头添加 */
@font-face {
  font-family: 'CustomFont';
  src: url('/fonts/custom-font.woff2') format('woff2');
  font-weight: 400;
  font-display: swap;
}

body {
  font-family: 'CustomFont', sans-serif;
}
```

字体文件放在 `static/fonts/` 目录。

---

## 🖼️ 字体推荐

### 中英文混排推荐组合

| 组合 | 英文 | 中文 | 风格 |
|------|------|------|------|
| **推荐 1** | Inter | PingFang SC | 现代简洁 |
| 推荐 2 | Source Sans Pro | Noto Sans SC | 开源友好 |
| 推荐 3 | Roboto | Microsoft YaHei | Windows 友好 |
| 推荐 4 | -apple-system | 系统默认 | 最快加载 |

### 代码字体推荐

| 字体 | 特点 |
|------|------|
| JetBrains Mono | 编程专用，连字支持 |
| Fira Code | 开源，连字支持 |
| Source Code Pro | Adobe 出品 |
| Monaco | macOS 默认 |
| Consolas | Windows 默认 |

---

## 🔧 常见问题

### Q1: 样式没有生效？

**检查清单：**
1. 文件路径是否正确：`assets/css/extended/custom.css`
2. 目录名是否正确：必须是 `extended`（不是 `extend`）
3. 清除缓存：`rm -rf public/ resources/`
4. 重启服务器

### Q2: 字体加载慢？

**解决方案：**
```css
/* 使用 font-display: swap 避免阻塞 */
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');
```

或改用系统字体，完全避免网络请求。

### Q3: 深色模式颜色不对？

确保 `.dark` 选择器中的变量正确：

```css
.dark {
  --link-color: var(--brand-secondary);  /* 使用亮色版 */
}
```

### Q4: 想单独调整某个页面？

使用更具体的选择器：

```css
/* 只调整首页 */
.home-info .entry-title {
  font-size: 2rem;
}

/* 只调整文章页 */
.post-single .post-content {
  font-size: 16px;
}
```

---

## 📊 效果对比

### 调整前 vs 调整后

| 方面 | 调整前 | 调整后 |
|------|--------|--------|
| **主题色** | 默认蓝色 | 品牌绿 #009873 |
| **正文字号** | 16px | 15px |
| **标题字号** | 较大 | 适中 |
| **字体** | 系统默认 | Inter + 苹方 |
| **链接色** | 蓝色 | 品牌绿 |
| **标签样式** | 灰色背景 | 品牌浅绿背景 |
| **代码高亮** | 默认 | 品牌色 |
| **选中文字** | 蓝色 | 品牌绿 |

---

## 🚀 快速开始

### 最简三步配置

**步骤 1**: 创建目录和文件
```bash
mkdir -p assets/css/extended
touch assets/css/extended/custom.css
```

**步骤 2**: 复制 CSS 内容
将 Artifact "PaperMod 自定义样式" 的完整内容复制到 `custom.css`

**步骤 3**: 重启查看效果
```bash
hugo server -D
```

完成！🎉

---

## 🎨 进阶定制

### 添加更多自定义

如果需要更多定制，可以创建多个 CSS 文件：

```
assets/css/extended/
├── custom.css       # 主要样式
├── fonts.css        # 字体定义
├── colors.css       # 颜色变量
└── animations.css   # 动画效果
```

PaperMod 会自动合并 `extended/` 目录下的所有 CSS 文件。

### 覆盖特定组件

如果需要完全重写某个组件，创建同名模板：

```
layouts/partials/
├── header.html      # 覆盖导航栏
├── footer.html      # 覆盖页脚
└── head.html        # 添加额外 head 内容
```

---

## 📱 响应式测试

调整后记得在不同设备测试：

- [ ] 桌面端 (1920×1080)
- [ ] 笔记本 (1366×768)
- [ ] 平板 (768×1024)
- [ ] 手机 (375×812)

可以使用浏览器开发者工具 (F12) 的设备模拟功能。

---

## 💡 其他建议

### 性能优化

1. **字体子集化**: 只加载需要的字符
2. **CSS 压缩**: Hugo 生产构建会自动压缩
3. **避免过多 @import**: 合并到一个文件

### SEO 友好

1. 保持良好的对比度（WCAG 标准）
2. 链接有明显的视觉区分
3. 字号不要太小（最小 14px）

---

需要我帮您调整任何具体的样式吗？